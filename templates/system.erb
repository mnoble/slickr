module Systems
  class <%= to_object(name) %>
    def call(delta, *args)
      # Called each tick, during the game loop
    end

    def entities
      # EntityManager.entities_with(:component)
    end
  end
end

